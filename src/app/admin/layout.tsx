export const metadata = {
  title: "Admin Panel",
  description: "Admin panel for creating and managing blog posts",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
